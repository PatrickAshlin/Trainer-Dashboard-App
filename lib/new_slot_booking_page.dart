import 'package:flutter/material.dart';

class NewSlotBookingPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('New Slot Booking')),
      body: Center(
        child: Text(
          'Booking Page Coming Soon!',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
